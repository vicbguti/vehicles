# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual Environments
.venv/
venv/
ENV/
env/

# Large Data Files (Do not commit large datasets)
data/raw/SRI_Vehiculos_Nuevos_*.csv
*.bak
data/raw/SRI_Vehiculos_DD.xlsx
data/processed/
data/features/
data/episodes/

# Profiling Cache (large autogenerated JSON files)
reports/cache/

# Model Artifacts (only the final run is versioned -- see docs/git_lfs.md)
artifacts/mlp/sweep/*
!artifacts/mlp/sweep/summary.json
artifacts/mlp/smoke200/

# Tooling Caches
.ruff_cache/
.pytest_cache/
*.egg-info/

# MLflow local tracking store. Una sola base, en la raíz del repositorio: la
# comparten el pipeline Kedro (MLFLOW_DB en pipelines/training/nodes.py) y
# scripts/train_classical.py. El sufijo -journal es de SQLite y aparece a mitad
# de una ejecución. `mlruns/` es el file store heredado, que ya no se usa.
mlflow.db
mlflow.db-journal
mlruns/
fleet_loading/mlflow.db

# Salidas intermedias del pipeline Kedro (se regeneran con `just train-fleet`)
fleet_loading/data/0[4578]_*/

# IDE and Editor Files
.vscode/
.idea/
.DS_Store
.ipynb_checkpoints/
site/
